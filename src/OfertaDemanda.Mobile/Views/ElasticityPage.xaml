<?xml version="1.0" encoding="UTF-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
             x:Class="OfertaDemanda.Mobile.Views.ElasticityPage">
  <Grid RowDefinitions="*,Auto" RowSpacing="12" Padding="12">
    <ScrollView Grid.Row="0" VerticalOptions="FillAndExpand">
      <VerticalStackLayout Spacing="10">
        <Label Text="{Binding Localization[Elasticity_UsesMarketDemand]}" FontAttributes="Bold" />
        <Label Text="{Binding Localization[Elasticity_Demand_Label]}" />
        <Entry Text="{Binding Market.DemandExpression}" />
        <Label Text="{Binding MarketShockText}" FontSize="12" />

        <Label Text="{Binding Localization[Elasticity_Price_Label]}" Margin="0,8,0,0" />
        <Slider Minimum="10" Maximum="110" Value="{Binding Price}" />
        <Label Text="{Binding PriceText}" FontSize="12" />

        <Label Text="{Binding ElasticityText}" Margin="0,8,0,0" />

        <Frame IsVisible="{Binding HasErrors}" BackgroundColor="#FFF0F0" Padding="8" CornerRadius="6">
          <VerticalStackLayout>
            <Label Text="{Binding Localization[Common_Errors]}" FontAttributes="Bold" />
            <VerticalStackLayout BindableLayout.ItemsSource="{Binding Errors}" Spacing="2">
              <BindableLayout.ItemTemplate>
                <DataTemplate>
                  <Label Text="• {Binding .}" />
                </DataTemplate>
              </BindableLayout.ItemTemplate>
            </VerticalStackLayout>
          </VerticalStackLayout>
        </Frame>
      </VerticalStackLayout>
    </ScrollView>

    <lvc:CartesianChart Grid.Row="1"
                        Series="{Binding Series}"
                        XAxes="{Binding XAxes}"
                        YAxes="{Binding YAxes}"
                        LegendPosition="Right"
                        HeightRequest="260" />
  </Grid>
</ContentView>
