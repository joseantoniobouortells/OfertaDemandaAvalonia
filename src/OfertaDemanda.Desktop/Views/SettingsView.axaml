<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:CompileBindings="False"
             mc:Ignorable="d"
             x:Class="OfertaDemanda.Desktop.Views.SettingsView">
  <Border Padding="24"
          CornerRadius="8"
          Background="{DynamicResource ThemeBackgroundBrush}"
          BorderBrush="{DynamicResource ThemeBorderMidBrush}"
          BorderThickness="1">
    <StackPanel Spacing="16">
      <StackPanel>
        <TextBlock Text="{Binding Localization[Settings_Title]}"
                   FontSize="20"
                   FontWeight="SemiBold" />
        <TextBlock Text="{Binding Localization[Settings_Subtitle]}"
                   Opacity="0.8"
                   Margin="0,4,0,0" />
      </StackPanel>

      <StackPanel Spacing="8">
        <TextBlock Text="{Binding Localization[Settings_Theme_Title]}"
                   FontWeight="Bold" />
        <ComboBox ItemsSource="{Binding ThemeOptions}"
                  SelectedItem="{Binding SelectedTheme, Mode=TwoWay}"
                  MinWidth="260"
                  HorizontalAlignment="Left">
          <ComboBox.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding Label}" />
            </DataTemplate>
          </ComboBox.ItemTemplate>
        </ComboBox>
        <TextBlock Text="{Binding Localization[Settings_Theme_Hint]}"
                  FontSize="12"
                  Opacity="0.9" />
      </StackPanel>

      <StackPanel Spacing="8">
        <TextBlock Text="{Binding Localization[Settings_Language_Title]}"
                   FontWeight="Bold" />
        <ComboBox ItemsSource="{Binding LanguageOptions}"
                  SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}"
                  MinWidth="260"
                  HorizontalAlignment="Left">
          <ComboBox.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding Label}" />
            </DataTemplate>
          </ComboBox.ItemTemplate>
        </ComboBox>
        <TextBlock Text="{Binding Localization[Settings_Language_Hint]}"
                   FontSize="12"
                   Opacity="0.9" />
      </StackPanel>

      <Border Padding="12"
              Background="{DynamicResource ThemeControlLowBrush}"
              CornerRadius="6">
        <StackPanel Spacing="4">
          <TextBlock Text="{Binding Localization[Settings_Config_Title]}"
                     FontWeight="Bold"
                     FontSize="13" />
          <TextBlock Text="{Binding SettingsFilePath}"
                     FontFamily="Consolas,Menlo,monospace"
                     TextWrapping="Wrap" />
          <TextBlock Text="{Binding Localization[Settings_Config_Hint]}"
                     FontSize="12"
                     Opacity="0.85" />
        </StackPanel>
      </Border>
    </StackPanel>
  </Border>
</UserControl>
